{
  "name": "raspored-local-server",
  "version": "1.0.0",
  "private": true,
  "description": "Lokalni SQLite backend za Raspored App",
  "type": "commonjs",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check apps-script-runtime.js && node --check import-google.js"
  }
}
